global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - "alert-manager:9093" 
rule_files:
  - alertrules.yml

scrape_configs:
  - job_name: 'node'
    file_sd_configs:
    - files:
      - 'targets.json'